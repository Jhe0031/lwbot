module.exports.run = (client, message, args) => {
      const Discord = require ('discord.js');

      if (message.guild.id === 
382585019300053013){
                               message.channel.send(new Discord.RichEmbed()
      .setTitle("Legal Info")
      .setDescription("Here you can find all needed legal information about this bot and the guild")
      .setColor(<yellow>)
);
      }
};